apiVersion: gate.v3.haproxy.org/v3
kind: HaproxyGateCtrlCfg
metadata:
  name: haproxyctrlconf
spec:
  logging:
    defaultLevel: "Debug"
    categoryLevelList:
      # - category: "gate"
      #   level: "Debug"
      # - category: "status"
      #   level: "Debug"
      - category: "k8s"
        level: "Info"
      - category: "batch"
        level: "Info"
