
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: route-backend-crd-extended
spec:
  parentRefs:
  - name: hug-gateway
    sectionName: http
  hostnames:
  - "backendcrdextended.haproxy.local"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: hello-world-backendcrd-extended-1
      port: 80
      filters:
      - type: ExtensionRef
        extensionRef:
          group: gate.v3.haproxy.org
          kind: Backend
          name: backend-cr-1
      - type: ExtensionRef
        extensionRef:
          group: gate.v3.haproxy.org
          kind: MergeType
          name: Override
      - type: ExtensionRef
        extensionRef:
          group: gate.v3.haproxy.org
          kind: Backend
          name: backend-cr-2
