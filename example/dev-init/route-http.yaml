apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: route-echo-http
spec:
  parentRefs:
  - name: hug-gateway
    sectionName: http
  hostnames:
  - "offload.haproxy"
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: http-echo
      port: 80
---
