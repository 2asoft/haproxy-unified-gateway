# Code generated by cmd/generate-gwapi; DO NOT EDIT.

tests-GW-API-1_3_0:
  needs: ["diff", "tidy"]
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: "$CI_PROJECT_NAMESPACE != 'haproxy-controller/community' && $CI_PIPELINE_SOURCE == 'push'"
  stage: unit-tests
  image:
    name: $CI_REGISTRY_GO/haproxy-alpine:$HAPROXY_VERSION-go$GO_VERSION
    entrypoint: [""]
  tags:
    - go
  before_script:
    - export PATH=$PATH:/root/go/bin
  script:
    - GWAPI_VERSION=1.3.0 task test
  artifacts:
    when: always
    paths:
      - junit-report.xml
    reports:
      junit: junit-report.xml

tests-GW-API-1_4_0:
  needs: ["diff", "tidy"]
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: "$CI_PROJECT_NAMESPACE != 'haproxy-controller/community' && $CI_PIPELINE_SOURCE == 'push'"
  stage: unit-tests
  image:
    name: $CI_REGISTRY_GO/haproxy-alpine:$HAPROXY_VERSION-go$GO_VERSION
    entrypoint: [""]
  tags:
    - go
  before_script:
    - export PATH=$PATH:/root/go/bin
  script:
    - GWAPI_VERSION=1.4.0 task test
  artifacts:
    when: always
    paths:
      - junit-report.xml
    reports:
      junit: junit-report.xml

tests-GW-API-1_5_0:
  allow_failure: true
  needs: ["diff", "tidy"]
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: "$CI_PROJECT_NAMESPACE != 'haproxy-controller/community' && $CI_PIPELINE_SOURCE == 'push'"
  stage: unit-tests
  image:
    name: $CI_REGISTRY_GO/haproxy-alpine:$HAPROXY_VERSION-go$GO_VERSION
    entrypoint: [""]
  tags:
    - go
  before_script:
    - export PATH=$PATH:/root/go/bin
  script:
    - GWAPI_VERSION=1.5.0 task test
  artifacts:
    when: always
    paths:
      - junit-report.xml
    reports:
      junit: junit-report.xml
unit-tests-todo:
  allow_failure: true
  needs: ["diff", "tidy"]
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: "$CI_PROJECT_NAMESPACE != 'haproxy-controller/community' && $CI_PIPELINE_SOURCE == 'push'"
  stage: unit-tests
  image:
    name: $CI_REGISTRY_GO/haproxy-alpine:3.2-go$GO_VERSION
    entrypoint: [""]
  tags:
    - go
  before_script:
    - export PATH=$PATH:/root/go/bin
  script:
    - task test-todo
  artifacts:
    when: always
    paths:
      - junit-report.xml
    reports:
      junit: junit-report.xml
