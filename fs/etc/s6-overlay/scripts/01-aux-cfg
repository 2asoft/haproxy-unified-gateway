#!/command/with-contenv sh

if [ ! -e /usr/local/unified-controller/aux/haproxy-aux.cfg ]; then
	touch /usr/local/unified-controller/aux/haproxy-aux.cfg
	chgrp haproxy /usr/local/unified-controller/aux/haproxy-aux.cfg
	chmod g+w /usr/local/unified-controller/aux/haproxy-aux.cfg
fi
