---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: haproxygatectrlcfgs.gate.v3.haproxy.org
spec:
  group: gate.v3.haproxy.org
  names:
    kind: HaproxyGateCtrlCfg
    listKind: HaproxyGateCtrlCfgList
    plural: haproxygatectrlcfgs
    singular: haproxygatectrlcfg
  scope: Namespaced
  versions:
  - name: v3
    schema:
      openAPIV3Schema:
        description: HaproxyGateCtrlCfg is a specification for a the controller related
          configuration
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              logging:
                properties:
                  categoryLevelList:
                    description: |-
                      CategoryLevelList is a list of categories and their levels
                      if a category is not present, the default level is used
                    items:
                      properties:
                        category:
                          enum:
                          - all
                          - k8s
                          - gate
                          - status
                          - haproxycfgmgr
                          type: string
                        level:
                          enum:
                          - Debug
                          - Info
                          - Warn
                          - Error
                          type: string
                      required:
                      - category
                      - level
                      type: object
                    type: array
                  defaultLevel:
                    enum:
                    - Debug
                    - Info
                    - Warn
                    - Error
                    type: string
                type: object
            required:
            - logging
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
