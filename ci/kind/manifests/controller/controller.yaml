apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: haproxy-controller
  name: haproxy-controller
  namespace: haproxy-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      run: haproxy-controller
  template:
    metadata:
      labels:
        run: haproxy-controller
    spec:
      serviceAccountName: haproxy-kubernetes-controller
      containers:
        - name: haproxy-controller
          image: haproxytech/kubernetes-controller:latest
          imagePullPolicy: Never
          resources:
            limits:
              memory: 2560Mi
            requests:
              memory: 2048Mi
          args:
            - --hugconf-crd=haproxy-controller/hugconf
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            runAsUser: 1000
            runAsGroup: 1000
            capabilities:
              drop:
                - ALL
              add:
                - NET_BIND_SERVICE
            seccompProfile:
              type: RuntimeDefault
          ports:
            - name: http
              containerPort: 30580
            - name: https
              containerPort: 30543
            - name: stat
              containerPort: 30524
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
---
apiVersion: v1
kind: Service
metadata:
  name: haproxy-kubernetes-controller
  namespace: haproxy-controller
spec:
  selector:
    run: haproxy-controller
  type: NodePort
  ports:
    - name: stat
      port: 30524
      targetPort: 30524
    - name: http
      port: 30580
      targetPort: 30580
      nodePort: 30580
      protocol: TCP
    - name: https
      port: 30543
      targetPort: 30543
      nodePort: 30543
      protocol: TCP
